name = "bomb-signal"
main = "worker.js"
compatibility_date = "2024-09-23"

# 房間用 Durable Object（每個房號一個實例，存連線清單）
[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

# 免費方案的 Durable Object 需用 SQLite 後端
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
